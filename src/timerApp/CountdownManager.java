package timerApp;

import javax.swing.*;
import java.awt.*;

public class CountdownManager {
    private static Timer countdownTimer;
    private static int countdownValue = 10;//The default countdown
    private static Color chosenColor = Color.BLUE;
    private static JFrame colorChangingFrame;
    private static int colorSpeed = 1000;//Rate of color change (ms)

}
